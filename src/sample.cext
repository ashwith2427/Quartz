import "io"

// Addition function.
/*
Simple prog lang for fun.
Done and dusted.
*/
add(uint32 a, uint32 b) -> uint32 = {
    uint32 temp = 892.99 + 567.55
    return a + b + '\n'
}

main() -> void = {
    uint32 addition = add(1, 3)
    print("{d}", addition)
}
